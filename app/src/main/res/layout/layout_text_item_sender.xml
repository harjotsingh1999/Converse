<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_sender_message_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="end"
    android:layout_marginVertical="10dp"
    android:layout_marginStart="100dp"
    android:layout_marginEnd="20dp"
    android:background="@drawable/chat_background"
    android:padding="10dp">

    <TextView
        android:id="@+id/sender_name_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="You"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/media_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_below="@id/sender_name_text_view"
        android:src="@drawable/chatting_image"
        android:visibility="gone" />


    <TextView
        android:id="@+id/item_sender_message_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/media_image"
        android:text="This is"
        android:textColor="@color/black" />

    <TextView
        android:id="@+id/item_sender_message_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/item_sender_message_text"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="10dp"
        android:text="10:20 am" />

</RelativeLayout>